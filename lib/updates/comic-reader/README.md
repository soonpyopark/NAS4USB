# Comic Reader (`comic-reader`)

만화·소설 리더 의존성 (npm).

| 패키지 | 용도 |
|--------|------|
| `epubjs` | EPUB 렌더 (`0.3.93`) |
| `7zip-min` | rar/cbr/7z 추출 (`3.0.1`, 7zip-bin) |

어댑터/셸: `src/lib/comicReader/`, `src/components/editors/ComicReaderShell.jsx`  
UI 영감: [Yomikiru](https://github.com/mienaiyami/yomikiru) (MIT, 포크 아님)

업데이트: `update_all.bat` / `npm run update:all` (EDITOR_CORES `comic-reader`)
